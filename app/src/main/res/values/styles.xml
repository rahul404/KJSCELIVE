<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">

    </style>

    <style name="MyToolbar" parent="@style/ThemeOverlay.AppCompat.Light">
        <!-- This will change drawer icon -->
        <item name="drawerArrowStyle">@style/MediumGrayDrawerIconStyle</item>
    </style>


    <style name="MediumGrayDrawerIconStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@color/colorMediumGray</item>
    </style>

    <style name="AppTabLayout" parent="Widget.Design.TabLayout">
        <!--<item name="tabMaxWidth">@dimen/tab_max_width</item>-->
        <item name="tabIndicatorColor">@color/colorBrightOrange</item>
        <item name="tabIndicatorHeight">2dp</item>
        <item name="tabPaddingStart">6dp</item>
        <item name="tabPaddingEnd">6dp</item>
        <item name="tabBackground">@color/colorLightGray</item>
        <item name="tabTextAppearance">@style/AppTabTextAppearance</item>
        <!--<item name="tabSelectedTextColor">@color/range</item>-->
    </style>

    <!-- for text -->
    <style name="AppTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">12sp</item>
        <!--<item name="android:textColor">@color/orange</item>-->
        <item name="textAllCaps">true</item>
    </style>

    <style name="LicensesTextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
    <style name="LicensesTextView.LicensesHeader" >
        <item name="android:textSize">35sp</item>
    </style>
    <style name="LicensesTextView.LicensesBody" >
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/colorMediumBlack</item>
    </style>
</resources>
